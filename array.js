const carrito = ['producto1', 'producto2', 'producto3']

const appConetenedor = document.querySelector("div")

let html = ''

/* carrito.forEach(producto => {
    html += `
        <li>${producto}</li>
        `
}) */
/* 
appConetenedor.innerHTML = html

carrito.map(producto => {
    return `El producto es ${producto}`
}) */

